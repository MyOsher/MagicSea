# Copy this file to `.env` and fill in your Copernicus Marine credentials.
# Register (free): https://data.marine.copernicus.eu/register
# `.env` is git-ignored — never commit real credentials.
COPERNICUSMARINE_SERVICE_USERNAME=your_username_here
COPERNICUSMARINE_SERVICE_PASSWORD=your_password_here
