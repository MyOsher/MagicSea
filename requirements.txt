# Week 1 — data handling only. Model deps (torch) come in weeks 3-4.
numpy>=1.24
xarray>=2023.1
netCDF4>=1.6
matplotlib>=3.7
pyyaml>=6.0
python-dotenv>=1.0

# Official Copernicus Marine download toolbox.
# Only needed once you have credentials and want REAL data.
copernicusmarine>=1.3
