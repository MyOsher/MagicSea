# Data & secrets — never commit (anchored to repo root so src/data/ is kept)
/data/
*.nc
*.npy
*.npz
.env

# Python
__pycache__/
*.pyc
.venv/
venv/
.ipynb_checkpoints/

# Model weights
*.pt

# Outputs (keep the folder, ignore heavy artifacts)
output/*.png
output/*.npy
output/*.npz
!output/.gitkeep
